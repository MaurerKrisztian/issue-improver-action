import { logLevelType } from "./Container";
export declare class Logger {
    private readonly ctx;
    private readonly logLevel;
    constructor(ctx: string, logLevel: logLevelType);
    private logStr;
    debug(msg: string, ctx?: string): void;
}
