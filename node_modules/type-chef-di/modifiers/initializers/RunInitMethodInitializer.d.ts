import { IResolver } from "../../interfaces/IResolver";
import { ArgResolver } from "../../definitions/helpers/ArgResolver";
import { IInitializer } from "./IInitializer";
export declare class RunInitMethodInitializer implements IInitializer {
    readonly resolver: IResolver;
    argResolver: ArgResolver;
    constructor(resolver: IResolver);
    run(resolvedInstance: any, definition: any): Promise<any>;
    runInitMethod(resolvedInstance: any, definition: any): Promise<any>;
}
