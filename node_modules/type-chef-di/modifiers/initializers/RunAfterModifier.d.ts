import { IResolver } from "../../interfaces/IResolver";
import { IInitializer } from "./IInitializer";
export declare class RunAfterModifier implements IInitializer {
    readonly resolver: IResolver;
    constructor(resolver: IResolver);
    run(instance: any, definition: any): Promise<any>;
    setRunAfter(resolvedInstance: any, definition: any): Promise<any>;
}
