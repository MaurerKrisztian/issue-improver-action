import { IInitializer } from "./initializers/IInitializer";
import { IResolver } from "../interfaces/IResolver";
import { IBaseDefinition } from "../definitions/definitionInterfaces/IBaseDefinition";
import { Type } from "../interfaces/IType";
export declare class Initializers {
    private readonly resolver;
    initializers: IInitializer[];
    constructor(resolver: IResolver, initializers?: IInitializer[]);
    addInitializers(InitializerTypes: Type<IInitializer>[]): void;
    runInitializers(instance: any, definition: IBaseDefinition): Promise<any>;
}
